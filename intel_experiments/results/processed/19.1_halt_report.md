# Phase 19.1 Halt Report

## Status

**HALT before completing the cross-placement matrix.**

The Phase 19.1 randomized matrix began after MLC validation passed. Completed
cells before halt:

| Cell | n | Mean cyc/load | Median cyc/load | Notes |
|------|---|---------------|-----------------|-------|
| AC-VC | 30 | 359.31 | 359.10 | Aggressor CXL, victim CXL |
| QC | 30 | 77.01 | 77.01 | CXL victim quiescent |
| AC-VL | 30 | 78.73 | 78.72 | Aggressor CXL, victim local |

The matrix halted when the fresh local baseline check showed AL-VL did not
reproduce the Phase 12 effect.

## Halt Trigger

Phase 19 halt condition:

```text
Phase 19.1 produces AL-VL tax substantially different from Phase 12 baseline
(e.g., AL-VL = 50% when Phase 12 was 197%) — requires investigation.
```

Fresh same-session checks:

| Check | n | Mean cyc/load | Median cyc/load |
|-------|---|---------------|-----------------|
| QL_REPRO | 10 | 76.95 | 76.94 |
| AL-VL_REPRO | 10 | 77.20 | 77.18 |

Fresh AL-VL tax:

```text
(77.20 / 76.95 - 1) x 100 = +0.32%
```

Phase 12 local baseline was approximately +197% (Q ~81.4, A ~241.8 cycles/load).
This session's AL-VL result is therefore not reproducible and cannot serve as
the denominator for DR3.

## Bandwidth Matching

Short WB+prefetch calibration:

| Placement | Aggressor cores | Median aggregate BW |
|-----------|-----------------|---------------------|
| Local DDR5 | 1 | 14.65 GB/s |
| CXL node 2 | 2 | 17.12 GB/s |

Using a 16 GB/s target, both are within +/-10% of target. The matrix CSV uses
these calibrated values in `agg_bw_gbps`. The raw aggressor stderr progress
metric is recorded separately as `agg_progress_bw_gbps_untrusted` because it is
known to be low-biased.

## Placement Evidence

For completed CXL cells, `/proc/PID/numa_maps` confirmed node-2 hugepage
residency. Example:

```text
victim: file=/anon_hugepage (deleted) huge anon=16 dirty=16 N2=16 kernelpagesize_kB=2048
aggressor: file=/anon_hugepage (deleted) huge anon=512 dirty=512 N2=512 kernelpagesize_kB=2048
```

## Outputs

```text
results/processed/19.1_crossplacement.csv        # partial: AC-VC, QC, AC-VL
results/processed/19.1_ql_repro_check.csv
results/processed/19.1_alvl_repro_check.csv
```

No DR3 verdict is valid from this partial matrix.
